interface SearchCriteries {
    bookName: string
    authorId: number
    genreId: number
    publisherId: number
    date: number
}

export const setCriteries = (props: []) => {
    let url = ""
    

}